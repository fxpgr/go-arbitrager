package usecase

/*
import (
	"testing"
	"github.com/facebookgo/inject"
	"github.com/fxpgr/go-arbitrager/domain/repository"
	"github.com/fxpgr/go-arbitrager/infrastructure/persistence"
)

func TestArbitrager(t *testing.T) {

	a := NewArbitrager(config, restService)
	isStarted, error := a.Arbitrage(opportunity)
	if error != nil {
		t.Fatalf("")
	}
}

func TestVulture(t *testing.T) {
	v := NewVulture(init_config)
	error := v.Execute()
	if error != nil {
		t.Fatalf("")
	}
}

func TestComponents(t *testing.T) {
	// t {Service Repository }
	t := NewTrader(init_config)buy, price, o,
	// trade
	error := t.Trade()
	if error != nil {
		t.Fatalf("")
	}
	IsDone, error := t.IsCompleted()
	if !IsDone {
		t.Fatalf("")
	}
	m := NewBearer(init_config)
	error := m.Transport()
	if error != nil {
		t.Fatalf("")
	}
	IsDone, error := m.IsCompleted()
	if !IsDone {
		t.Fatalf("")
	}

	// -> timeoutはArbitragerレベルで管理する
	// あくまでtraderは取引のみを責務とする
	// -> doneの場合はtrueを返す
	// Arbitragerは外から確認する
}
*/
